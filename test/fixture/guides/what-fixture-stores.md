---
title: What fixture stores
description: "Plain rows, one per call, nothing derived."
---

**Every call writes one row** with the arguments and the exit status.

## Where on disk?

Under the user config directory, in `fixture/store.db`.
